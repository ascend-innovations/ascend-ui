import { it, expect, describe } from 'vitest'
import { isValidDateRange } from '$lib/index.js'


describe('tests isValidDateRange utility', () => {
    it('detects valid date range', () => {
        
    })
})